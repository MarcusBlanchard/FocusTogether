[package]
name = "focustogether-native-host"
version = "0.1.0"
description = "Native messaging host for FocusTogether Chrome extension"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
dirs = "5.0"
